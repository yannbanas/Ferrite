//! Owned by Agent 2, alongside `ferrite-catalog`. Lexer + parser for the
//! Ferrite v1 SQL dialect subset (see workspace README for exact scope)
//! producing an AST consumed by `ferrite-planner`. Depends on nothing but
//! `ferrite-common` — this crate has no notion of storage or execution.

pub struct NotYetImplemented;
