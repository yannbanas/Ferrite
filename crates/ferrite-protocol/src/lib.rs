//! Owned by Agent 4, alongside `ferrite-server`. Implements the
//! PostgreSQL frontend/backend protocol version 3 (startup, simple and
//! extended query flow, auth) over `tokio`, with TLS on by default (see
//! workspace README §Security model — TLS is not an opt-in flag).

pub struct NotYetImplemented;
