[package]
name = "ferrite-proc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Triggers and stored procedures engine. Also where row-level access control lives: procedures receive the caller's Identity and decide access in code, rather than a separate declarative RLS policy language. Owned by Agent 3."
readme = "README.md"

[dependencies]
ferrite-common.workspace = true
tracing.workspace = true
