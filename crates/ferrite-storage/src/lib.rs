//! Owned by Agent 1. Implements `ferrite_common::StorageEngine`: page
//! format, B-tree, MVCC row versions, and a crash-recovery journal. See
//! the workspace README for scope (checksummed pages on by default, no
//! transaction-id wraparound handling in v1).

pub struct NotYetImplemented;
